import { ReactNode } from 'react';
import { Youtube, Facebook, Instagram, Twitter, MessageCircle } from 'lucide-react';
// Note: TikTok isn't available in Lucide icons by default


export const SHOP_INTEL_ADMIN_URL = process.env.SHOP_INTEL_ADMIN_URL || process.env['Shop-Intel_ADMIN_URL'];
export const ShopIntel_ADMIN_URL = SHOP_INTEL_ADMIN_URL; // Backward compatibility (renamed from Shop-Intel_ADMIN_URL)